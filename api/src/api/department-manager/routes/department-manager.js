'use strict';

/**
 * department-manager router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::department-manager.department-manager');
